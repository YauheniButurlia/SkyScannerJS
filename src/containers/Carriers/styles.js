import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    container: { backgroundColor: '#ff4081', flex: 1 },
    item: {
      padding: 10,
      fontSize: 18,
      height: 44,
      borderWidth: 2
  },
  });